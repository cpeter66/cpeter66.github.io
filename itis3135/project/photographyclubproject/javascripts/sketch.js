function setup() {
    createCanvas(600, 400);
    background(240);
    textSize(64);
    textAlign(CENTER, CENTER);
    fill(34, 139, 34);
    text('UNCC', width / 2, height / 2);
  }
  
  function draw() {
    // Could add animation or interaction here if desired
  }
  